import { SplitBeneficiosPipe } from './split-beneficios.pipe';

describe('SplitBeneficiosPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitBeneficiosPipe();
    expect(pipe).toBeTruthy();
  });
});
