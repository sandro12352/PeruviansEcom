import { ProductoNombrePipe } from './producto-nombre.pipe';

describe('ProductoNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new ProductoNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
